package inmem

/*
import (
	"fmt"
	"testing"
)

func TestSimpleCreate(t *testing.T) {
    fs := New()

    fs.mkdir(fs.root, split(fs.normPath("a/b/c")))

    tree := &dirNode{
    fmt.Println(fs.root)
}
*/
